//actions are just objects 

//when writing actions in redux we create something called acr=tion creater function that return an object

export const incrementCounter=()=>{
    return{
        type:"INCREMENT"
    }
}

